# Project-specific: private data and cached models.
# `figures/` is INTENTIONALLY NOT gitignored — generated figures are committed
# to the repo so readers can browse them on GitHub without running notebooks.
data/
models/
!data/.gitkeep
!models/.gitkeep

# CLAUDE.local.md is the session-scoped working doc; only the short public CLAUDE.md is committed.
CLAUDE.local.md

# macOS
.DS_Store
**/.DS_Store

# Python
__pycache__/
*.py[codz]
*.so
*.egg-info/
*.egg
build/
dist/

# Environments
.env
.venv
venv/

# Jupyter
.ipynb_checkpoints

# Caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
